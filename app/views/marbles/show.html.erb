<p id="notice"><%= notice %></p>

<h2><%= @marble.marble_name %></h2>
  <%= image_tag @marble.avatar.url(:medium) %>

<div class="show actions">
  <%= link_to 'Edit', edit_marble_path(@marble) %>
  <%= link_to 'Remove', @marble, data: { confirm: 'Are you sure you want to delete a marble?' }, method: :delete, class: 'destroy' %>
  <%= link_to 'Back to all my marbles', marbles_path %>
</div>

<p>
  <strong>User:</strong>
  <%= @marble.user.user_name %>
</p>

<p>
  <strong>Marble Pic:</strong>
  friend.avatar.url => http://marbletrader.s3.amazonaws.com/...
  <%#= image_tag friend.avatar.url(:square) %>
</p>

<p>
  <strong>Marble name:</strong>
  <%= @marble.marble_name %>
</p>

<p>
  <strong>Marble text:</strong>
  <%= @marble.marble_text %>
</p>

<%= link_to 'Edit', edit_marble_path(@marble) %> |
<%= link_to 'Back', marbles_path %>
<%= link_to "Sign out",destroy_user_session_path, method: :delete %>
